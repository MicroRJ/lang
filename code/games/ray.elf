
log ?= pf

log("loading ray...")
let enum _time = clocktime()
finally log(" loaded ray, took: ",timediffs(_time))

raylib ?= elf.loadlib("build/raylib.exe")


lgi_initWindowed = lgifn("raylib_load")
